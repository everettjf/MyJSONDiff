{
  "name": "MyJSONDiff",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "cd src && npm run dev",
    "build": "cd src && npm run build",
    "preview": "cd src && npm run start",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-opener": "^2"
  },
  "devDependencies": {
    "typescript": "~5.6.2",
    "@tauri-apps/cli": "^2"
  }
}
